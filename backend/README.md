# Impact Arlington API

Requires Node 22.5+ (uses the built-in SQLite module).

```bash
npm run init-db
npm run dev
```

The SQLite database is `cms.sqlite`. The local seed administrator is `admin@impactarlington.org` / `ImpactAdmin123!`; change this before deployment. Set `PORT` and `CLIENT_ORIGIN` as needed.
